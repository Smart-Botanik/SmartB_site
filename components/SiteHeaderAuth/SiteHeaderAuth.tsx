"use client";

type SiteHeaderAuthProps = {
  variant?: "desktop" | "mobile";
};

/**
 * Header auth CTA — hidden until social / login module starts.
 * Restore UI from git history when wiring real session.
 */
export function SiteHeaderAuth(_props: SiteHeaderAuthProps) {
  return null;
}
